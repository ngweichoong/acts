# Isaiah preview notes

This branch is intentionally isolated from `main` so the scripture visual can be reviewed safely before merge.

## Changes
- Adds a compact Isaiah 41:18–20 reflection block to `pages/chapter2.html`.
- Adds `isaiah.css` with the generated scripture-inspired landscape embedded directly in CSS.
- Adds a subtle animated water-highlight effect to suggest flowing waterfalls and streams.
- Respects `prefers-reduced-motion` by disabling the animation.

## Rollback
If the result is not approved, close the PR and delete this branch. `main` remains unchanged.
